Make the single-element attach() call unambiguous. With clang the braced
initializer {&text_color} matches both attach(StateColor const &) and
attach(std::vector<StateColor const *> const &); GCC happens to pick the
vector overload. Spell out the vector, which is what is meant.

--- a/src/slic3r-shared-wx/src/Slic3r/App/WX/Widgets/Button.cpp	1970-01-01 00:00:01.000000000 +0000
+++ b/src/slic3r-shared-wx/src/Slic3r/App/WX/Widgets/Button.cpp	2026-09-02 20:07:22.618396238 +0000
@@ -49,7 +49,7 @@
 bool Button::Create(wxWindow* parent, wxString text, wxString icon, long style, wxSize iconSize/* = wxSize(16, 16)*/)
 {
     StaticBox::Create(parent, wxID_ANY, wxDefaultPosition, wxDefaultSize, style);
-    state_handler.attach({&text_color});
+    state_handler.attach(std::vector<StateColor const *>{&text_color});
     state_handler.update_binds();
     wxWindow::SetLabel(text);
     if (!icon.IsEmpty()) {
